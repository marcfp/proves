#include<iostream>
#include<stdlib.h>
#include <stdio.h>
#include <string.h>
#include <vector>
#include <algorithm>
using namespace std;
int area(int i);
int main()
{
	int s1 = area (7);
int s2 = area (7);
int s3 = area (7);
int s4 = area (7);
// error:) missing
// error:; missing
// error:Int is not a type
// error:non-terminated character('missing)
return(0);
}
int area(int i)
{
	return (i*i);
}
